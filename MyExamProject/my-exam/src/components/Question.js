
import React, { useStateContext } from '../hooks/useStateContext'

export default function Question() {
    const { context, stateContext } = useStateContext(stateContext)



    return (
        <div>Question</div>
    )
}
